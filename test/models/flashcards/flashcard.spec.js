define(['models/flashcards/flashcard'], function (FlashcardsFlashcard) {
  describe('FlashcardsFlashcard', function () {
    it('exists', function () {
      expect(FlashcardsFlashcard).to.be.ok;
    });
  });
});