define(['views/flashcards/add'], function (FlashcardsAddView) {
  describe('FlashcardsAddView', function () {
    it('exists', function () {
      expect(FlashcardsAddView).to.be.ok;
    });
  });
});